import { StaticPage } from '../../components/static-page';

export default function ReturnsPage() {
  return (
    <StaticPage
      eyebrow="Support"
      title="Returns"
      description="Returns guidance for sealed products, accessories, and other eligible orders."
    >
      <p>We would normally place the live returns policy here. This route is ready for the final policy copy.</p>
    </StaticPage>
  );
}
