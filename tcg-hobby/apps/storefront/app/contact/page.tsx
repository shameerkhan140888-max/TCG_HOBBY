import { StaticPage } from '../../components/static-page';

export default function ContactPage() {
  return (
    <StaticPage
      eyebrow="Support"
      title="Contact"
      description="Need help with an order, release, buylist, or account? This placeholder page points the way."
    >
      <p>Email support would normally live here for launch. For now, the page exists so the footer has a real target.</p>
    </StaticPage>
  );
}
