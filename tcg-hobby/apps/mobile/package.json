{
  "name": "@tcg-hobby/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "node ./scripts/start-expo.mjs",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tcg-hobby/types": "*",
    "expo": "^54.0.0",
    "react": "19.1.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@tcg-hobby/config": "*",
    "@types/react": "~19.1.10",
    "typescript": "~5.9.2"
  }
}
